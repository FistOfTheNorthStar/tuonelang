[package]
name = "tuo-package"
description = "Package and build orchestration tooling for tuonelang."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
# The package system is data-and-filesystem only: it models the manifest and
# lockfile, resolves a path-dependency graph off disk, and computes content
# checksums, all in plain values. It holds no compiler machinery — a host loads
# the resolved sources into its own pipeline — so it needs no tuonelang crate.
