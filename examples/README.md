# tuonelang examples

This directory will hold small, illustrative tuonelang programs once the language can
compile them.

tuonelang is not yet a usable language, so there are no runnable examples. Any file
added here is illustrative only until the compiler can validate it — see
`../corpus/README.md` for how programs become trusted.
