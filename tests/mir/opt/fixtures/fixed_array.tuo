// ADR-0004 Stage 2: a fixed array's length is lowered as a constant, never a
// `Rvalue::Len`, so the optimizer's plain constant folding can fold every
// `i < N` bounds check to `const true` without any array-specific knowledge.
fn pick() -> Int {
    let xs = [4, 5, 6];
    xs[0] + xs[2]
}
